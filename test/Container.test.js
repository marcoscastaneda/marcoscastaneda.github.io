import { h, render } from 'preact';
// import test from 'ava';

import Container from '../src/components/Container';

// test('Contains child element', t => {
//   const wrapper = render(<Container />);
//   t.is(wrapper.to.have.length(1));
// });
